FROM ubuntu

ENV TZ=Asia/Shanghai

WORKDIR /app
COPY  app/permission/api/permission-api /app/permission-api
COPY app/permission/api/etc /app/etc
COPY common/casbin /common/casbin

RUN chmod +x /app/permission-api

EXPOSE 8889

CMD ["./permission-api", "-f", "etc/permission-api.yaml"]