FROM ubuntu

ENV TZ=Asia/Shanghai

WORKDIR /app
COPY  app/user/rpc/user-rpc /app/user-rpc
COPY app/user/rpc/etc /app/etc
COPY common/casbin /common/casbin

RUN chmod +x /app/user-rpc

EXPOSE 18888

CMD ["./user-rpc", "-f", "etc/user-rpc.yaml"]