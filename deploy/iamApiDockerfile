FROM ubuntu

ENV TZ=Asia/Shanghai

WORKDIR /app
COPY  app/iam/api/iam-api /app/iam-api
COPY app/iam/api/etc /app/etc
COPY common/casbin /common/casbin

RUN chmod +x /app/iam-api

EXPOSE 8889

CMD ["./iam-api", "-f", "etc/iam-api.yaml"]